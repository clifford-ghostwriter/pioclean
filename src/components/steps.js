import React from "react";
import styled from "styled-components";

const Steps = () => {
  return (
    <Wrapper className="section-center" id="learnmore">
      <div>steps</div>
    </Wrapper>
  );
};

const Wrapper = styled.div``;
export default Steps;
